# SPDX-License-Identifier: PMPL-1.0-or-later
name: "🟡 CHECK: OSSF Scorecard"

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]
  schedule:
    - cron: '0 4 * * *'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  security-events: write
  id-token: write

jobs:
  scorecard:
    name: OSSF Scorecard Check
    uses: hyperpolymath/standards/.github/workflows/scorecard-reusable.yml@main
    permissions:
      contents: read
      security-events: write
      id-token: write
